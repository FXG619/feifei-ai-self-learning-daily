# Roadmap

## 阶段一：半自动 AI 自学力日报站

AI 生成 `draft`，飞飞审核后改为 `published`。首页优先呈现最新一篇已发布日报，内容重点是“今天只做一件事”。

## 阶段二：加入固定可信信源摘要

接入少量固定可信信源摘要，但所有外部信源内容只进入 `draft`，不得自动公开发布。

## 阶段三：增强传播和检索

加入分享卡片、RSS、搜索、标签页和用户信源提报。RSS 只包含 `published` 内容。

## 阶段四：付费学员自学雷达

接入付费学员的中考英语 AI 自学雷达，但必须另行设计数据隐私、学生数据保护和人工审核机制。
