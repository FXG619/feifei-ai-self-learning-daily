{
  "name": "feifei-ai-self-learning-daily",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "generate:draft": "node --experimental-strip-types scripts/generate-draft.ts",
    "validate:content": "node --experimental-strip-types scripts/validate-content.ts && astro check"
  },
  "dependencies": {
    "astro": "^5.13.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
